from tkinter import *

def criar_tela_csv(frame_conteudo):

    titulo = Label(
        frame_conteudo,
        text="Importar / Exportar CSV",
        font=("Arial", 20, "bold")
    )
    titulo.pack(pady=20)